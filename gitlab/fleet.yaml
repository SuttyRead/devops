namespace: gitlab
helm:
  releaseName: gitlab
  chart: "gitlab"
  repo: "https://charts.gitlab.io"
  version: ""
  force: true
  values:
    global:
      edition: ce
      hosts:
        domain: sutty.shop
