namespace: gitlab
helm:
  releaseName: gitlab
  chart: "gitlab"
  repo: "https://charts.gitlab.io"
  version: "7.6.1"
  force: true
  values:
    global:
      edition: ce
      hosts:
        domain: sutty.shop
