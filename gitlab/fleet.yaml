namespace: gitlab
helm:
  releaseName: gitlab
  chart: "gitlab/gitlab"
  repo: "https://charts.gitlab.io/"
  version: "latest"
  force: false
  timeoutSeconds: 0
  values:
    global:
      edition: ce
      hosts:
        domain: sutty.shop
